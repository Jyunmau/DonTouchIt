<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jyunmauchan.dontouchit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activitys.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activitys.TaskActivity" />
        <activity
            android:name=".activitys.AboutActivity"
            android:label="About" />
        <activity android:name=".activitys.TimerActivity" />
        <activity android:name=".activitys.FinishedActivity"
            android:label="Finished"/>
        <activity android:name=".activitys.LotteryActivity"
            android:label="Lottery"/>
    </application>

</manifest>